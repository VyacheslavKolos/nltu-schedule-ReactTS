import React from 'react';
import {Box, Stack, Typography} from "@mui/material";

const Session = () => {
    return (
        <Stack alignItems={'center'}>
            <Typography variant={'h4'} fontWeight={'bold'}>Люде, сесія не скоро, схаменіться : )</Typography>
        </Stack>
    );
};

export default Session;
